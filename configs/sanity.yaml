data:
  root: data/tiny5
  split: train
  num_frames: 8
  image_size: 224

model:
  backbone: resnet18
  pretrained: false
  num_classes: 101
  dropout: 0.0

train:
  seed: 42
  device: cpu
  batch_size: 2
  num_workers: 0
  lr: 0.0003
  weight_decay: 0.0001
  max_steps: 3
